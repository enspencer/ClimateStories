<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <% if @story.user %>
  	<%= @story.user.username %>
  <% else %>
  	Anonymous
  <% end %>
</p>

<p>
  <strong>Name:</strong>
  <%= @story.name %>
</p>

<p>
  <strong>Body:</strong>
  <%= @story.body %>
</p>

<p><img src="<%= @story.url %>" width=300px /></p>

<%# if user_owns_story? %>
	<%= link_to 'Edit', edit_story_path(@story) %> |
	<%= link_to 'Destroy', @story, method: :delete, data: { confirm: 'Are you sure?' } %> |
	<%= link_to 'Back', stories_path %>
<%# else %>
	<%#= link_to 'Back', stories_path %>
<%# end %>

<div style='width: 800px;'>
  <div id="map" style='width: 1100px; height: 400px;'></div>
</div>

<%= gmaps4rails(@json) %>