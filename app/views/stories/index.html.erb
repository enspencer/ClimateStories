<p class="lead">What are the effects of climate change on your home? <a href="/stories/new">Add your story</a> to the map and share it with the world.</p>
<div class="map">

	<div id="map" style='width: 1200px; height: 525px;'></div>

</div>

<h3>All stories:</h3>
	<% @stories.each do |story| %>
	<div class="index-story">
	<h4><%= link_to story.name, story %></h4>
	<h6><%= story.body %></h6>
</div>
<% end %>

<%= gmaps("markers" => {"data" => @json},
                "map_options" => {"type" => "TERRAIN", :raw => '{mapTypeControlOptions: {mapTypeIds: ["watercolor", google.maps.MapTypeId.TERRAIN], style: google.maps.MapTypeControlStyle.DROPDOWN_MENU}}'}) %>

                </div>